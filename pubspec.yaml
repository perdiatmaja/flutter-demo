name: atmaja_flutter_demo
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_mobx: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  json_serializable: ^6.1.1
  flutter_dotenv: ^5.0.2
  build_runner: ^2.1.5
  mobx_codegen: ^2.0.4
  json_annotation: ^4.4.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - .env.local
