import 'package:flutter/material.dart';

class NavigatorPush extends StatelessWidget {
  const NavigatorPush({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) =>
      const Text("Welcome to new Screen after push");
}
